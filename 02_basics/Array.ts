// const name : type[] = []
const superHeroes: string[] = [];
// const heroPower : number[] = []  OR
const heroPower: Array<number> = [];

superHeroes.push("spiderman");
heroPower.push(2);

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLmodels: number[][] = [[255, 255, 255], []];

allUsers.push({ name: "", isActive: true });